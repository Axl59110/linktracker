/**
 * AlpineJS initialization for Blade layouts
 * Used for: mobile menu drawer, dropdowns, modals, etc.
 */

import Alpine from 'alpinejs';

// Initialize Alpine
window.Alpine = Alpine;
Alpine.start();
